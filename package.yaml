name: nuxt-template
private: true
type: module

devEngines:
  packageManager:
    name: pnpm
    version: 10.32.1+sha512.a706938f0e89ac1456b6563eab4edf1d1faf3368d1191fc5c59790e96dc918e4456ab2e67d613de1043d2e8c81f87303e6b40d4ffeca9df15ef1ad567348f2be
    onFail: download

engines:
  node: '>= 24'

scripts:
  build: nuxt build
  dev: nuxt dev
  generate: nuxt generate
  preview: nuxt preview
  postinstall: nuxt prepare
  typecheck: nuxt typecheck

  prepare: husky

  clean: node scripts/clean.ts

  test:unit:watch: vitest
  test:unit: vitest --run
  test:e2e:ui: playwright test --ui
  test:e2e: playwright test

  lint: pnpm lint:es && pnpm lint:style
  lint:es: eslint --max-warnings 0
  lint:style: stylelint "**/*.{css,less,scss,sass,styl,stylus,pcss,postcss,sss,vue}"
  lint:fix: pnpm lint:es:fix && pnpm lint:style:fix
  lint:es:fix: pnpm lint:es --fix
  lint:style:fix: pnpm lint:style --fix

  # Task to create package.json for tools that need it
  compile:packageJson: node scripts/compile-package-json.ts

  # Workaround for:
  # - ESLint requiring a package.json
  # - Vitest to load ESM modules, as it needs to see "type": "module" in package.json
  # - Nuxt needing package.json to typecheck properly
  pnpm:devPreinstall: pnpm compile:packageJson

dependencies:
  '@nuxt/fonts': ^0.14.0
  '@nuxt/image': ^2.0.0

  vue-dompurify-html: ^5.3.0
  isomorphic-dompurify: ^3.3.0

  nuxt: ^4.4.2
  vue: ^3.5.30
  vue-router: ^5.0.3

devDependencies:
  # For linting
  '@nuxt/eslint': ^1.15.2
  eslint: ^10.0.3
  eslint-plugin-unicorn: ^63.0.0
  eslint-plugin-vue-pug: ^0.6.2 # ESLint rules for Pug
  '@vitest/eslint-plugin': ^1.6.12 # ESLint rules for Vitest
  eslint-plugin-playwright: ^2.9.0 # ESLint rules for Playwright
  eslint-plugin-testing-library: ^7.16.0 # ESLint rules for Testing Library
  '@eslint/eslintrc': ^3.3.5 # To convert eslint-plugin-vue-pug to new ESLint flat config format
  yaml: ^2.8.2

  # Enable use of TypeScript config files in various tools
  jiti: ^2.6.1

  # To use Sass
  sass-embedded: ^1.98.0

  # For testing
  '@nuxt/test-utils': ^4.0.0
  '@playwright/test': ^1.58.2
  '@vue/test-utils': ^2.4.6
  '@testing-library/vue': ^8.1.0
  jsdom: ^28.1.0
  std-env: ^4.0.0
  vitest: ^4.0.18
  'monocart-reporter': ^2.10.0

  # For code quality checks on pre-commit
  husky: ^9.1.7
  lint-staged: ^16.3.3

  # To enable typechecking
  typescript: ^5.9.3
  vue-tsc: &vue-tools ^3.2.5
  '@types/node': ^24.12.0 # Allow typechecking of node code as well

  # To use Pug in Vue templates
  '@vue/language-plugin-pug': *vue-tools # Enables autocompletion for Vue components in Pug templates
  pug: ^3.0.4

  # For linting stylesheets
  '@nuxtjs/stylelint-module': ^5.2.1
  stylelint: ^17.4.0
  stylelint-define-config: ^17.2.0
  '@stylelint-types/stylelint-scss': ^7.0.0
  '@stylelint-types/stylelint-stylistic': ^5.0.0
  stylelint-config-standard-scss: ^17.0.0
  stylelint-config-standard-vue: ^1.0.0
  '@stylistic/stylelint-config': ^4.0.0
  # Use SugarSS as a pseudo-sass parser due to postcss-sass not working with media queries
  postcss-sass: npm:sugarss@latest
  postcss-html: ^1.8.1
